#include "markup.h"

/*Markup::Markup()
{
}

Markup::Markup(ToolType tooltype, int key, double height, Marktype marktype, QColor rgba)
{

}

void Markup::read(const QJsonObject &json)
{
    tooltype    = json["tooltype"].toString();
    key         = json["key"].toInt();
    height      = json["height"].toDouble();
    notes       = json["notes"].toString();
    marktype    = json["marktype"].toString();
}

void Markup::write(QJsonObject &json) const
{
    json["color"]       = color;
    json["tooltype"]    = tooltype;

}*/
